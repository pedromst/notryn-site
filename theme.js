'use strict';
// The presentation opens in the app's default Glass palette. Demo choices
// are temporary and must not inherit or overwrite saved app preferences.
window.NotrynThemes.apply(window.NotrynThemes.presets.find(theme => theme.id === 'glass'));
